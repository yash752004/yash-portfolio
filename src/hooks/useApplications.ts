// Deprecated: Applications are now handled via direct email (connect@pinaktechnology.com)
export const useApplications = () => {
  return {
    applications: [],
    loading: false,
    addApplication: async () => {},
    updateApplicationStatus: async () => {},
    deleteApplication: async () => {}
  };
};
